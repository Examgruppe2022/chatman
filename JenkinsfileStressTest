pipeline {
    agent any
    triggers {
        pollSCM "0 0 * * *"
    }
    stages{
        stage("Stress testing the API") {
            sh"this should run on the production container, however, since that dose not work right now, it will instead run on the test container"
            dir("performance tests") {
                sh"k6 run -e URL=185.51.76.42:8091 stressTest.js"
            }
        }
    }
}